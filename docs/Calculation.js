// Keep this from the legacy Calculation.js file.
var CalcInput = pojo('Ynow', 'Yinf', 's_eq', 's_lower', 's_upper', 's_step','exponential','S1Included','Omega');

// Import the rest from the LightConeCalc module.
const { Calculate, CalculateTage, ScaleResults } = LightConeCalc;
